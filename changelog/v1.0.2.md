## Highlights

- Microsoft Entra settings now use a cleaner, more consistent layout around migration controls and the HTTPS helper.

## Changes

- Admin/authentication: removed redundant Entra helper copy, aligned the collapsed `Migration policy` card with the Entra field grid, kept `Sign out from Microsoft session too` anchored beside it, and kept the HTTPS helper concise with a direct `Open HTTPS & Proxy` shortcut.
