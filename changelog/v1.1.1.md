## Highlights

- Docker deployment templates now use `default.env.example` consistently in the repository, release assets, and setup documentation.

## Changes

- Deployment: the tracked env template was renamed from `.env.example` to `default.env.example`.
- Deployment: release packaging now keeps the same `default.env.example` naming in generated assets.
- Documentation: quick-start and release guidance now reference `default.env.example` consistently.
