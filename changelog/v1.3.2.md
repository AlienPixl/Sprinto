## Highlights

- Room queue pagination sizing is now a little more conservative so the last visible issue row no longer collides with the pagination controls on shorter desktop heights.

## Bug Fixes

- Fixed the room queue page-size calculation so the bottom queue item does not visually overlap the `Previous / Next / count` bar in the bottom-right corner.
